#Calculate frequency of characters in a string
str1 = input()
freq = {}
for ch in str1:
  if ch in freq:
    freq[ch]+=1
  else:
    freq[ch]=1
    
for ch,count in freq.items():
  print(f"{ch},{count}")
