#Remove all duplicates from the input string.
string = input()
res = ''
seen = set()
for ch in string:
  if ch not in seen:
    res+=ch
    seen.add(ch)
print(res)


#using dictionary
string = input()
dic ={}
for ch in string:
  dic[ch]=1

res = ''.join(dic.keys())
print(res)
    
